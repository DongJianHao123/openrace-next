import LoginPage from "@/views/LoginPage";

export const metadata = {
    title: "登录 - openrace",
    description: "登录 openrace 账号",
}

const Page = () => {
    return (
            <LoginPage />
    )
}

export default Page;